export enum TodoStatus {
  IN_PROGRESS = "in-progress",
  DONE = "done",
  TODO = "todo",
}

export enum TodoPriority {
  HIGH = "high",
  MEDIUM = "medium",
  LOW = "low",
}

export enum SelectedPeriod {
  WEEKLY = "high",
  MONTHLY = "low",
  YEARLY = "medium",
}
